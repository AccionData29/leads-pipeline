# Fuentes de los diagramas

Los diagramas de esta carpeta representan la arquitectura propuesta para el proyecto Leads y están alineados con la separación:
Angular → .NET → Python → PostgreSQL.

Los SVG son generados como artefactos estáticos para documentación y no dependen de Mermaid.
